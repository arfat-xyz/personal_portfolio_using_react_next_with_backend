import React from "react";
import { Section, SectionTitle } from "../styles/GlobalComponents";

const blogs = () => {
  return (
    <Section>
      <SectionTitle main>Comming soon...</SectionTitle>
    </Section>
  );
};

export default blogs;
